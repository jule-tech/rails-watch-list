<h1>Lists</h1>
<ul>
  <% @lists.each do |list| %>
    <li>
      <h4><%= list.name %> | <%= link_to 'Show list', list_path(list) %></h4>
    </li>
  <% end %>
</ul>
<%= link_to "Create a new list", new_list_path(@list), class: "btn btn-primary"%>
